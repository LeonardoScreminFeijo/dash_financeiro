import { formatCurrency, formatDate } from "@/lib/formatters";
import type { Transaction } from "@/types/transaction";

export function TransactionsTable({ transactions }: { transactions: Transaction[] }) { return <section className="overflow-hidden rounded-2xl border border-stone-200 bg-white shadow-sm"><div className="p-5"><h2 className="text-base font-semibold">Movimentações</h2><p className="text-sm text-stone-500">{transactions.length} resultado(s)</p></div><div className="overflow-x-auto"><table className="min-w-full text-left text-sm"><thead className="border-y border-stone-200 bg-stone-50 text-xs uppercase tracking-wide text-stone-500"><tr><th className="px-5 py-3">Data</th><th className="px-5 py-3">Descrição</th><th className="px-5 py-3">Categoria</th><th className="px-5 py-3">Conta</th><th className="px-5 py-3">Pagamento</th><th className="px-5 py-3 text-right">Valor</th></tr></thead><tbody>{transactions.map((item, index) => <tr key={`${item.date}-${item.time}-${item.description}-${index}`} className="border-b border-stone-100 last:border-0"><td className="whitespace-nowrap px-5 py-3">{formatDate(item.date)}</td><td className="px-5 py-3 font-medium">{item.description}<span className="ml-2 text-xs text-stone-400">{item.installment}</span></td><td className="px-5 py-3">{item.category}</td><td className="px-5 py-3">{item.account}</td><td className="px-5 py-3">{item.paymentMethod}</td><td className={`whitespace-nowrap px-5 py-3 text-right font-semibold ${item.type === "income" ? "text-pine" : "text-coral"}`}>{item.type === "income" ? "+" : "−"} {formatCurrency(item.amount)}</td></tr>)}</tbody></table></div></section>; }
